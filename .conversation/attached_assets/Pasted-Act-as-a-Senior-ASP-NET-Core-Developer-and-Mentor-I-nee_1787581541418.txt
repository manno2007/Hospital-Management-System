Act as a Senior ASP.NET Core Developer and Mentor. I need you to generate a complete, working ASP.NET Core MVC 8 application for a "Hospital Management System". 



CRITICAL CONSTRAINTS & ARCHITECTURE:

- DO NOT use the Repository Pattern or a separate Services layer. All Controllers MUST inject and use `AppDbContext` directly.

- Use Entity Framework Core with SQLite database.

- Use standard Data Annotations for validation (`[Required]`, `[StringLength]`, `[Display]`, etc.) inside the models.

- Implement the standard 7 CRUD actions (Index, Details, Create GET/POST, Edit GET/POST, Delete GET/POST) in the Controllers.

- Apply security best practices: Use `[ValidateAntiForgeryToken]` for all POST requests and `[Bind]` to prevent over-posting.

- Follow the Post/Redirect/Get (PRG) pattern and use `TempData["Success"]` for success messages.

- The UI should be built with Razor Views and Bootstrap 5 (RTL layout is preferred if you generate Arabic text, otherwise standard Bootstrap is fine).



MODELS & RELATIONSHIPS:

1. Department: Id, Name. (One-to-Many with Doctors).

2. Patient: Id, Name, RegistrationDate. (One-to-Many with Appointments).

3. Doctor: Id, Name, DepartmentId. (One-to-Many with Appointments. Navigation property to Department).

4. Appointment: Id, AppointmentDate, Diagnosis/Description, PatientId, DoctorId. (Navigation properties to Patient and Doctor).



OUTPUT REQUIREMENTS:

Please generate the code step-by-step, providing the full content for each file. Do not skip or abbreviate the implementation. Structure your response in this exact order:

1. The 4 Model classes (Department.cs, Patient.cs, Doctor.cs, Appointment.cs).

2. Data/AppDbContext.cs (Include the DbSet properties, `OnModelCreating` to configure the relations explicitly using Fluent API, and add some Seed Data).

3. Program.cs (Configured with SQLite and routing).

4. The Controllers (DepartmentsController, DoctorsController, PatientsController, AppointmentsController) showing proper `Include()` for Eager Loading where necessary.

5. The essential Views (show an example of `Index` and `Create` for the `Appointments` to demonstrate how the Dropdowns for Doctors and Patients are populated using `ViewBag`). 

