kind = "web"
previewPath = "/"
title = "Hospital Management System"
description = "A hospital operations app for managing departments, doctors, patients, and appointments with secure CRUD workflows and a clear clinical overview."
version = "1.0.0"
id = "artifacts/hospital-management"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20044

[services.development]
run = "pnpm --filter @workspace/hospital-management run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/hospital-management", "run", "build" ]
serve = "static"
publicDir = "artifacts/hospital-management/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20044"
BASE_PATH = "/"
